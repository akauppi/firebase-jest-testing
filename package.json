{
  "name": "@local/root",
  "description": "Root - just managing the project.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm --prefix sample install"
  }
}
