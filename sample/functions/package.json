{
  "name": "functions",
  "private": true,
  "//type": "module",
  "engines": {
    "node": "14 || >= 15"
  },
  "dependencies": {
    "firebase-admin": "^9.5.0",
    "firebase-functions": "^3.13.1"
  },
  "//": {
    "engines.node": [
      "Firebase Cloud Functions only supports LTS versions (12,14,...) and the string must be '12','14', ...",
      "We get away with '14 || >= 15' since this is not being deployed. '14' gives warnings",
      ">= 13.2 provides native ES module support"
    ]
  }
}
